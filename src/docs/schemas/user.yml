components:
  schemas:
    User:
      type: object
      properties:
        email:
          type: string
          example: 'fake@example.com'
        token:
          $ref: "#components/schemas/Token"